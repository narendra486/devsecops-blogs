---
id: iac-security
title: IaC Security
description: Scan Terraform, Kubernetes YAML, and cloud configuration.
---

# IaC Security

## Why This Matters

Infrastructure code can create public cloud risk very quickly.

## Actual Security Problem

A small Terraform change can open SSH to the internet or disable encryption.

In real implementation, the issue is not only the tool. The main issue is where the check runs, what permission it gets, and whether the result blocks risky code.

## How Attackers Misuse It

Attackers search for exposed storage, public databases, weak IAM, and open network rules.

They do not always need a zero-day. Many times they use normal engineering paths like pull requests, package updates, workflow changes, or leaked credentials.

## Common Wrong Setup

Teams review application code carefully but merge Terraform changes without security checks.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: unsafe-app
spec:
  containers:
    - name: app
      image: nginx:latest
      securityContext:
        privileged: true
```

What is wrong here:

- The workload runs with unsafe privilege.
- The image tag is not pinned.
- A small config change can create a cloud or cluster risk.
- This should fail in PR before deployment.

## Practical Implementation

```yaml
name: iac-security
on:
  pull_request:
permissions:
  contents: read
  security-events: write
jobs:
  trivy-config:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: config
          format: sarif
          output: trivy-config.sarif
      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-config.sarif
```

## Useful Commands

```bash
checkov -d .
trivy config .
tfsec .
```

## Example Result

```text
Checkov failed: security group allows 0.0.0.0/0 on port 22.
```

## Secure Fix

Run IaC scanning in PR and block critical cloud misconfigurations.

Also make sure the check is required in GitHub rulesets or branch protection. Otherwise it only reports and does not stop risky code.

## Practical Recommendations

- Start with high-confidence checks.
- Keep permissions minimum.
- Put security feedback in the pull request.
- Make only important checks blocking in the beginning.
- Tune false positives before strict enforcement.
- Track exceptions with owner, reason, and expiry.
